# ملاحظات النشر والروابط العامة

تُنشئ الأداة الآن رابط مشاركة نظيفًا بالشكل `/i/{publicId}`. عند ربط المشروع بنطاقك الخاص، يظهر للمستخدم رابط مثل `https://images.example.com/i/abc123` ولا يظهر مسار `/manus-storage/` في الرابط الذي ينسخه المستخدم.

## النشر عبر الاستضافة المدمجة مع نطاق مخصص

هذا هو المسار الأنسب للإصدار الحالي. الموقع يحتاج إلى خادم لتشغيل رفع الصور، حفظ بيانات الرابط، وتقديم المسار `/i/{publicId}` كصورة مباشرة. بعد ربط نطاقك الخاص من إعدادات النطاقات ونشر المشروع، يأخذ الرابط تلقائيًا اسم النطاق الذي فتحت منه الأداة.

## GitHub Pages

GitHub Pages يستضيف ملفات ثابتة فقط، ولذلك لا يشغّل خادم TypeScript ولا tRPC ولا قاعدة البيانات ولا مسار الصورة المباشر. إذا نُشر المشروع الحالي على GitHub Pages وحده فستتوقف وظيفة رفع الصورة وإنشاء الرابط؛ كما أن GitHub Pages لا يغيّر روابط التخزين التي أُنشئت سابقًا.

إذا كان GitHub مطلوبًا، استخدمه لمستودع الكود فقط، أو انقل الخلفية والتخزين إلى خدمة خلفية مستقلة على نطاقك الخاص. تحتاج هذه البنية إلى ثلاث طبقات: واجهة ثابتة، واجهة API للرفع وإدارة `publicId`، وتخزين كائنات للصور. لا تستخدم GitHub Pages وحده لهذه الوظيفة.
