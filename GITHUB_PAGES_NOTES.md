# GitHub Pages وYazin-link

GitHub Pages يمكنه استضافة **واجهة ثابتة** باسم نطاق GitHub أو نطاقك المخصص، ولا يضيف تلقائيًا عبارة أو شارة باسم Manus إلى كود الموقع. سجل **روابطي** في هذا الإصدار يستخدم التخزين المحلي للمتصفح، ولذلك لا يحتاج إلى حساب؛ لكنه يظل خاصًا بالجهاز والمتصفح نفسه.

لكن GitHub Pages لا يشغّل خادم Node أو قاعدة بيانات أو رفع ملفات. لذلك لا يمكنه وحده تنفيذ رفع الصورة الحالي أو إنشاء رابط صورة دائم من المسار `/i/{id}`. لتشغيل هذه الوظيفة تحت نطاقك الخاص دون إظهار نطاق المنصة، انقل الرفع والتخزين ومسار الرابط إلى واجهة API وتخزين كائنات مستقلين ثم اربطهما بنطاقك.
